<h1>IP Geolocations</h1>

<%= form_with scope: :ip_geolocation, url: ip_geolocations_path, local: true do |form| %>

  <% if @ip_geolocation.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ip_geolocation.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @ip_geolocation.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :ip_address %><br>
    <%= form.text_field :ip_address %>
  </p>

  <p>
    <%= form.label :notes %><br>
    <%= form.text_area :notes %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to 'Back', ip_geolocations_path %>
